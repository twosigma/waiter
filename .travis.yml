language: clojure
env:
  matrix:
    - TEST_DIR=waiter DEPS_CMD='lein with-profiles +test voom build-deps' TEST_CMD='lein test'
before_script: (cd $TEST_DIR && $DEPS_CMD)
script: cd $TEST_DIR && $TEST_CMD
