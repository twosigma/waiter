#!/usr/bin/env python3

from setuptools import setup

requirements = [
    'numpy',
    'tensorflow'
]

setup(
    name='tensorflow-image-tagging',
    version='1.0',
    packages=[],
    entry_points={},
    install_requires=requirements
)
